# NAVEGACIÓN (3 comandos)
pwd                    # Dónde estoy
ls                     # Qué hay aquí
cd carpeta             # Ir a carpeta

# GIT (5 comandos) — CRÍTICO
git status             # Ver cambios
git add .              # Agregar todo
git commit -m "msg"    # Guardar
git push               # Enviar a GitHub
git pull               # Traer cambios

# PYTHON (3 comandos)
python3 archivo.py     # Ejecutar código
pip install libreria   # Instalar paquete
python3 -m venv venv   # Crear entorno

# ARCHIVOS (4 comandos)
mkdir carpeta          # Crear carpeta
touch archivo.py       # Crear archivo
rm archivo             # Borrar
nano archivo           # Editar

# ADMIN (2 comandos)
sudo comando           # Como admin
sudo apt install pkg   # Instalar programa

# BÚSQUEDA (2 comandos)
find . -name "*.py"    # Buscar archivos
grep "texto" archivo   # Buscar en archivo

# OTROS (1 comando)
clear                  # Limpiar pantalla